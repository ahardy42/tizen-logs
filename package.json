{
  "name": "tizen-logs",
  "version": "1.0.0",
  "description": "component which allows you to view console logs on Samsung tv",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./lib && tsc",
    "prepare": "npm run build"
  },
  "author": "Andy Hardy",
  "license": "ISC",
  "types": "lib/index.d.ts",
  "peerDependencies": {
    "react": "^15.x || ^16.x"
  },
  "dependencies": {
    "@types/react": "^16.9.41",
    "console-feed": "^3.0.0"
  }
}
